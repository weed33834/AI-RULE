# Engineering（工程实现）

> **适用场景**: 架构落地、编码、配置、部署时
> **输入/输出契约**: 输入: 需求 + 技术栈 → 输出: 代码 + 配置 + 部署方案

## 规则

1. 最小变更：只改用户指定的文件，不顺手优化无关代码
2. 有成熟库必须用库，禁止手写底层逻辑
3. 防御性编程只在可预见错误处使用，不包 try-except 一切
4. 代码注释只写'为什么'，不写'什么'
5. 禁止硬编码密钥，必须用环境变量
