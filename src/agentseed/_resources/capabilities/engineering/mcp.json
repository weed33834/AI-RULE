{
  "name": "engineering",
  "version": "1.0.0",
  "description": "Engineering capability: 代码生成、Git 工作流、工作流五子角色、技能获取五层协议",
  "tools": [
    {
      "name": "validate_codebase",
      "description": "按编码标准（ruff/mypy/pytest）校验代码库，返回问题清单",
      "parameters": {
        "type": "object",
        "properties": {
          "path": { "type": "string", "description": "目标代码目录绝对路径" },
          "checks": { "type": "array", "items": { "type": "string", "enum": ["ruff", "mypy", "pytest"] }, "description": "要运行的检查项" }
        },
        "required": ["path"]
      }
    },
    {
      "name": "git_status",
      "description": "执行 git status + git diff 检查，确保提交前无临时文件/密钥夹带",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  ],
  "resources": ["coding_standards", "git_sop", "workflow_five_roles", "skill_acquisition_protocol"]
}
