{
  "_comment": "Gemini CLI Hook 配置模板。复制到 .gemini/hooks.json 或 ~/.gemini/settings.json",
  "_setup": "1) 把 adapters/hooks/ 目录复制到你的项目根目录 2) 安装 Rule Hub（pip install ai-rule 或设 AI_RULE_REPO）3) 重启 Gemini CLI",
  "PreToolUse": [
    {"command": "python ./adapters/hooks/gemini/pre_tool_use.py"}
  ]
}
