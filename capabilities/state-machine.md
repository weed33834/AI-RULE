# State Machine（状态机治理）

> **适用场景**: 分支可达性检查、状态迁移验证、非法迁移阻断时
> **输入/输出契约**: 输入: 状态迁移表 + 不变量 → 输出: 合法迁移序列 + 冲突报告

## 规则

1. 状态机是唯一叙事事实来源，不与文本描述冲突
2. 非法迁移必须阻断，不静默接受
3. 每次输出包含：当前状态、状态变化、可选分支
4. 分支可达性检查：所有可达状态都有路径到达
5. 存档/读档必须保证状态完整可恢复
