{
  "name": "testing",
  "version": "1.0.0",
  "description": "Testing capability: 测试生成、覆盖率分析、pytest 集成",
  "tools": [
    {
      "name": "generate_tests",
      "description": "为指定模块生成 pytest 测试用例，覆盖正常路径/边界/异常",
      "parameters": {
        "type": "object",
        "properties": {
          "module_path": { "type": "string", "description": "目标 Python 模块绝对路径" },
          "coverage_target": { "type": "number", "default": 80, "description": "目标覆盖率百分比" }
        },
        "required": ["module_path"]
      }
    }
  ],
  "resources": ["test_templates", "coverage_config"]
}
