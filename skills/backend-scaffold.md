---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 98adaa6f98486df4666888a6691e7607_c1757d1f883611f18108525400287e28
    ReservedCode1: yyXbyk21kVxjSJUXMUPxdd5+wS28IkJcVm0RNHmZVG7r/QwvWhPYYCQGIwHuHDFI/tkZi+MczLywmoj71BsREEFBbi/s/ji5jx+A6vPIOk4URDLwqVGrD2P9srfUvYVdZybmxg2nQfgSP10P7ZDVTcghDVCXdKRdlHzjYQskezQr9/52E3gRVQjVRZ8=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 98adaa6f98486df4666888a6691e7607_c1757d1f883611f18108525400287e28
    ReservedCode2: yyXbyk21kVxjSJUXMUPxdd5+wS28IkJcVm0RNHmZVG7r/QwvWhPYYCQGIwHuHDFI/tkZi+MczLywmoj71BsREEFBbi/s/ji5jx+A6vPIOk4URDLwqVGrD2P9srfUvYVdZybmxg2nQfgSP10P7ZDVTcghDVCXdKRdlHzjYQskezQr9/52E3gRVQjVRZ8=
---

# Backend Scaffold — 后端 API 三层架构脚手架

> **触发条件**：需要创建新 API 端点或新模块时。
> **加载时机**：Engineer 角色根据 Architect 的设计方案实现后端代码时。
> **优先级**：P2 — 确保代码结构一致、可测试、可维护。

---

## 架构总览

```text
 ┌──────────────────────────────────────────┐
 │              Router (路由层)               │
 │  接收 HTTP 请求 → 参数校验 → 调用 Service  │
 ├──────────────────────────────────────────┤
 │              Schema (校验层)               │
 │  请求体 / 响应体 / 查询参数 类型定义和校验   │
 ├──────────────────────────────────────────┤
 │               Model (数据层)               │
 │  ORM 模型 / 数据库表映射                    │
 └──────────────────────────────────────────┘
```

---

## 技术栈适配矩阵

| 技术栈 | Model | Schema | Router | 测试 |
|--------|-------|--------|--------|------|
| FastAPI (Python) | SQLAlchemy ORM | Pydantic v2 | APIRouter | pytest + httpx |
| Express (Node.js) | Prisma / TypeORM | Zod | express.Router | jest + supertest |
| Django (Python) | Django ORM | DRF Serializer | ViewSet / APIView | pytest-django |
| Flask (Python) | SQLAlchemy | Marshmallow | Blueprint | pytest |

---

## 生成步骤

### Input Contract（输入契约）

```yaml
module_name: "users"          # 模块名称（kebab-case）
fields:                        # 字段定义
  - name: "email"
    type: "string"
    constraints: ["required", "unique", "email_format"]
  - name: "display_name"
    type: "string"
    constraints: ["required", "max_length=100"]
  - name: "is_active"
    type: "boolean"
    constraints: ["default=true"]
tech_stack: "FastAPI"         # FastAPI / Express / Django
base_path: "/api/v1/users"    # API 路由前缀
auth_required: true            # 是否需要认证
```

---

## 文件生成清单

每次脚手架生成以下 4 个文件（以 FastAPI + SQLAlchemy 为例）：

```
src/
├── models/
│   └── user.py          # Model: ORM 模型
├── schemas/
│   └── user.py          # Schema: Pydantic 请求/响应校验
├── routes/
│   └── user.py          # Router: API 端点 + 业务逻辑
└── tests/
    └── test_user.py     # Tests: pytest 测试骨架
```

---

## File 1: Model（数据模型）

### 模板（FastAPI + SQLAlchemy）

```python
# src/models/user.py
# Module: User
# Generated by: backend-scaffold skill

from sqlalchemy import Column, Integer, String, Boolean, DateTime
from sqlalchemy.sql import func
from src.database import Base

class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    email = Column(String(255), unique=True, nullable=False, index=True)
    display_name = Column(String(100), nullable=False)
    is_active = Column(Boolean, default=True, nullable=False)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), onupdate=func.now())

    def __repr__(self):
        return f"<User(id={self.id}, email={self.email})>"
```

### 约束
- 所有字段必须显式定义 `nullable` 和 `default`
- 字符串字段必须指定 `length`
- 关联字段必须定义 `index=True`（查询优化）
- 时间戳字段必须含时区（`timezone=True`）

---

## File 2: Schema（校验层）

### 模板（FastAPI + Pydantic v2）

```python
# src/schemas/user.py
# Module: User
# Generated by: backend-scaffold skill

from datetime import datetime
from pydantic import BaseModel, EmailStr, Field

# ── Request Schemas ────────────────────────────

class UserCreate(BaseModel):
    """创建用户的请求体"""
    email: EmailStr
    display_name: str = Field(..., min_length=1, max_length=100)
    is_active: bool = Field(default=True)

class UserUpdate(BaseModel):
    """更新用户（所有字段可选）"""
    email: EmailStr | None = None
    display_name: str | None = Field(None, min_length=1, max_length=100)
    is_active: bool | None = None

# ── Response Schemas ────────────────────────────

class UserResponse(BaseModel):
    """用户响应体（不含敏感字段）"""
    id: int
    email: str
    display_name: str
    is_active: bool
    created_at: datetime
    updated_at: datetime | None = None

    model_config = {"from_attributes": True}

class UserListResponse(BaseModel):
    """分页列表响应"""
    items: list[UserResponse]
    total: int
    page: int
    page_size: int
```

### 约束
- Request 和 Response Schema 分离
- Response Schema 必须 `from_attributes=True`（ORM 模式）
- 数字字段使用 `Field(ge=0)` 防止负数
- 字符串字段必须限制长度（防止 DoS）

---

## File 3: Router（路由 + 业务逻辑）

### 模板（FastAPI）

```python
# src/routes/user.py
# Module: User
# Generated by: backend-scaffold skill

from fastapi import APIRouter, Depends, HTTPException, Query, status
from sqlalchemy.ext.asyncio import AsyncSession
from src.database import get_db
from src.models.user import User
from src.schemas.user import (
    UserCreate,
    UserUpdate,
    UserResponse,
    UserListResponse,
)
from src.auth.dependencies import get_current_user

router = APIRouter(prefix="/api/v1/users", tags=["users"])


@router.post("/", response_model=UserResponse, status_code=status.HTTP_201_CREATED)
async def create_user(
    payload: UserCreate,
    db: AsyncSession = Depends(get_db),
):
    """创建新用户"""
    existing = await db.execute(
        select(User).where(User.email == payload.email)
    )
    if existing.scalar_one_or_none():
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT,
            detail=f"User with email '{payload.email}' already exists",
        )

    user = User(**payload.model_dump())
    db.add(user)
    await db.commit()
    await db.refresh(user)
    return user


@router.get("/", response_model=UserListResponse)
async def list_users(
    page: int = Query(default=1, ge=1),
    page_size: int = Query(default=20, ge=1, le=100),
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """获取用户列表（需认证）"""
    # ... implementation
    pass


@router.get("/{user_id}", response_model=UserResponse)
async def get_user(
    user_id: int,
    db: AsyncSession = Depends(get_db),
):
    """获取单个用户"""
    user = await db.get(User, user_id)
    if not user:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"User {user_id} not found",
        )
    return user


@router.patch("/{user_id}", response_model=UserResponse)
async def update_user(
    user_id: int,
    payload: UserUpdate,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """更新用户（需认证）"""
    # ... implementation
    pass


@router.delete("/{user_id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_user(
    user_id: int,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """删除用户（需认证）"""
    # ... implementation
    pass
```

### 约束
- CRUD 端点统一：`POST /` (create), `GET /` (list), `GET /{id}` (read), `PATCH /{id}` (update), `DELETE /{id}` (delete)
- 列表端点必须分页（`page` + `page_size`）
- 创建返回 201，删除返回 204
- 错误信息统一使用 `detail` 字段，包含具体上下文
- 业务逻辑不应超过 20 行（复杂逻辑抽取到 service 层）

---

## File 4: Tests（测试骨架）

### 模板（pytest + httpx）

```python
# tests/test_user.py
# Module: User
# Generated by: backend-scaffold skill

import pytest
from httpx import AsyncClient, ASGITransport
from src.main import app


@pytest.fixture
async def client():
    """创建测试客户端"""
    async with AsyncClient(
        transport=ASGITransport(app=app),
        base_url="http://test",
    ) as ac:
        yield ac


@pytest.mark.asyncio
class TestUserCreate:
    """POST /api/v1/users"""

    async def test_create_user_success(self, client):
        """正常创建用户 → 201"""
        response = await client.post(
            "/api/v1/users",
            json={
                "email": "test@example.com",
                "display_name": "Test User",
            },
        )
        assert response.status_code == 201
        data = response.json()
        assert data["email"] == "test@example.com"
        assert data["display_name"] == "Test User"
        assert data["is_active"] is True

    async def test_create_user_duplicate_email(self, client):
        """重复邮箱 → 409"""
        payload = {"email": "dup@example.com", "display_name": "Dup"}
        await client.post("/api/v1/users", json=payload)
        response = await client.post("/api/v1/users", json=payload)
        assert response.status_code == 409

    async def test_create_user_invalid_email(self, client):
        """无效邮箱 → 422"""
        response = await client.post(
            "/api/v1/users",
            json={"email": "not-an-email", "display_name": "Bad"},
        )
        assert response.status_code == 422


@pytest.mark.asyncio
class TestUserRead:
    """GET /api/v1/users"""

    async def test_get_user_not_found(self, client):
        """不存在的用户 → 404"""
        response = await client.get("/api/v1/users/99999")
        assert response.status_code == 404


@pytest.mark.asyncio
class TestUserList:
    """GET /api/v1/users"""

    async def test_list_users_empty(self, client):
        """空列表 → 200 + items=[]"""
        response = await client.get("/api/v1/users")
        assert response.status_code == 200
        data = response.json()
        assert data["items"] == []
        assert data["total"] == 0
```

### 测试覆盖要求

| 端点 | 最少测试数 | 必须覆盖 |
|------|-----------|----------|
| CREATE | 3 | 正常创建、重复冲突、字段校验 |
| READ | 2 | 正常读取、NotFound |
| LIST | 2 | 空列表、分页 |
| UPDATE | 2 | 正常更新、NotFound |
| DELETE | 2 | 正常删除、NotFound |

---

## 注册到主路由

生成模块后必须修改主路由注册文件（如 `src/main.py`）：

```python
# 在 src/main.py 中添加
from src.routes.user import router as user_router
app.include_router(user_router)
```

**约束（core/governance.md §4）**：只修改主路由注册文件中的 `include_router` 部分，不修改其他内容。

---

## 禁止行为

| 禁止 | 原因 |
|------|------|
| 在 Router 中直接写原始 SQL | 应通过 ORM |
| Schema 中不限制字符串长度 | DoS 风险 |
| 不生成测试文件 | 无测试 = 不可验证 |
| 修改主路由以外的不相关文件 | core/governance.md §4 变更范围 |
| 跳过注册步骤 | 路由不注册 = 端点不可用 |

---

## 交叉引用

| 引用 | 内容 |
|------|------|
| `core/governance.md §4` | 变更范围：只改指定文件（model + schema + router + tests） |
| `core/governance.md §1` | 安全与保密：不硬编码密钥、数据库密码 |
| `core/governance.md §2` | 真实性：API 响应基于真实数据库查询 |
| `skills/workflow-five-roles.md` | 五子角色：Engineer 实现后 Critic 审查 |
| `skills/skill-acquisition.md` | 选库协议：ORM / 校验库选择走五层协议 |
*（内容由AI生成，仅供参考）*
